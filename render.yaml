services:
  - type: web
    name: movie-finder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn flask_app:app --timeout 180 --workers 1
    plan: starter
